<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Event_manager"
        tools:targetApi="31">
        <activity
            android:name=".activities.Accounts.SignInActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.NavigationDrawer.NavigationDrawer" />
        <activity android:name=".activities.TemplatePattern.RequestedEventsActivity" />
        <activity android:name=".activities.BookmarksActivity" />
        <activity android:name=".activities.EventBoard.EventBoardActivity" />
        <activity android:name=".activities.Accounts.SignUpActivity" />
        <activity android:name=".activities.EventMaker.AddEventActivity" />
        <activity android:name=".activities.EventMaker.EventPageActivity"></activity>
    </application>

</manifest>